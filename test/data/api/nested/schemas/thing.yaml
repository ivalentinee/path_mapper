thing:
  title: Thing
  type: object
  additionalProperties: false
  required: ['name']
  properties:
    name: { type: string, minLength: 1 }
    tag: { $ref: '#/tag' }
tag:
  type: string
  enum: ['red', 'blue']
