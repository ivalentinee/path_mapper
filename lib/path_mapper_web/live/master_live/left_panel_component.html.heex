<div class="left-panel" phx-click="noop" phx-target={@myself}>
  <div class="panel-selector">
    {panel_select_button(%{
      text: gettext("Adventures"),
      panel_name: "adventure-selector",
      myself: @myself,
      left_panel_state: @left_panel_state
    })}
    {panel_select_button(%{
      text: gettext("Groups"),
      panel_name: "group-selector",
      myself: @myself,
      left_panel_state: @left_panel_state
    })}

    <%= if @group && @game_state do %>
      {panel_select_button(%{
        text: gettext("Scenes"),
        panel_name: "scene-selector",
        myself: @myself,
        left_panel_state: @left_panel_state
      })}
    <% end %>

    <%= if @game_state && @game_state.scene do %>
      {panel_select_button(%{
        text: gettext("Map"),
        panel_name: "map-manager",
        myself: @myself,
        left_panel_state: @left_panel_state
      })}
    <% end %>

    <%= if @game_state && @game_state.scene do %>
      {panel_select_button(%{
        text: gettext("Tokens"),
        panel_name: "tokens",
        myself: @myself,
        left_panel_state: @left_panel_state
      })}
    <% end %>
  </div>

  <%= if selected_panel(@left_panel_state) == "adventure-selector" do %>
    <div class="panel-content">
      <.live_component
        module={PathMapperWeb.MasterLive.LeftPanelComponent.AdventureSelectorComponent}
        id="adventure-selector"
        adventures={@adventures}
        adventure={@adventure}
      />
    </div>
  <% end %>

  <%= if selected_panel(@left_panel_state) == "group-selector" do %>
    <div class="panel-content">
      <.live_component
        module={PathMapperWeb.MasterLive.LeftPanelComponent.GroupSelectorComponent}
        id="group-selector"
        groups={@groups}
        group={@group}
      />
    </div>
  <% end %>

  <%= if @group && @adventure && @game_state && selected_panel(@left_panel_state) == "scene-selector" do %>
    <div class="panel-content">
      <.live_component
        module={PathMapperWeb.MasterLive.LeftPanelComponent.SceneSelectorComponent}
        id="scene-selector"
        left_panel_state={@left_panel_state}
        adventure={@adventure}
        game_state={@game_state}
      />
    </div>
  <% end %>

  <%= if @game_state && @game_state.scene && selected_panel(@left_panel_state) == "map-manager" do %>
    <div class="panel-content">
      <.live_component
        module={PathMapperWeb.MasterLive.LeftPanelComponent.MapManagerComponent}
        id="map-manager"
        left_panel_state={@left_panel_state}
        adventure={@adventure}
        game_state={@game_state}
      />
    </div>
  <% end %>

  <%= if @adventure && @game_state && selected_panel(@left_panel_state) == "tokens" do %>
    <div class="panel-content">
      <.live_component
        module={PathMapperWeb.MasterLive.LeftPanelComponent.TokensComponent}
        id="tokens"
        left_panel_state={@left_panel_state}
        adventure={@adventure}
        game_state={@game_state}
        group={@group}
      />
    </div>
  <% end %>
</div>
